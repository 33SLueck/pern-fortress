model {{pascalName}} {
  id        Int      @id @default(autoincrement())
{{#each fields}}
  {{name}}  {{type}}{{#unless required}}?{{/unless}}{{#if unique}} @unique{{/if}}
{{/each}}
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  @@map("{{tableName}}")
}