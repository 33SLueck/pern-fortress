// This is your Prisma schema file, // learn more about it in the docs:
https://pris.ly/d/prisma-schema model
{{pascalName}}
{ id Int @id @default(autoincrement())
{{#each fields}}
  {{name}}
  {{type}}{{#if required}}{{/if}}{{#if unique}} @unique{{/if}}
{{/each}}
createdAt DateTime @default(now()) updatedAt DateTime @updatedAt @@map("{{tableName}}")
}